# ======== UEP EVENT ORGANIZER BACKEND ========
# Add this file to your production server as '.env'

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (Update these with your Production Database details)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=programmer
DB_NAME=uep_event_booking_db

# Security & Authentication (MUST CHANGE IN PRODUCTION)
JWT_SECRET=generatethiswitharandomstringtoolandkeepitsecret123!
JWT_EXPIRES_IN=86400
BCRYPT_SALT_ROUNDS=10
